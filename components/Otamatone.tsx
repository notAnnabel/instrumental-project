// import { otamatone }from "@/components/Piano";
import { Orientation } from "expo-screen-orientation";

export default function Piano() {
  return (
stem: {
    position: "relative",
    width: 75,
    height: 200,
    borderWidth: 2,
    borderColor: "black",
    backgroundColor: "purple", // use the variable in use state variable
    top: -50,
    zIndex: 1,
    left: 100,
  }
  )}